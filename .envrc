# Phase 20.5 — direnv config for kubewp-runtime.
# No ./bin wrappers needed today (no host-side pnpm/go invocations possible).
# PATH_add is here for future-proofing if/when scripts get added.

if [[ -d ./bin ]]; then
  PATH_add ./bin
fi
